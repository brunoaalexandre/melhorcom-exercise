import styled from "styled-components";

export const Container = styled.header`
  width: 100%;
  background: var(--blue);
  padding: 1.25rem 0;

  display: flex;
  align-items: center;
  justify-content: center;

  img {
    max-height: 63px;
  }
`;
